# vf.defs

## Measure (measure)

- `hasNumericalValue` (Required): integer
  - The numeric value (numerator).
- `hasDenominator`: integer
  - The denominator for fractional values. Default 1 if omitted.
- `hasUnit`: string (at-uri)
  - Reference to a Unit record.

---

