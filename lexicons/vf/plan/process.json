{
  "lexicon": 1,
  "id": "vf.plan.process",
  "defs": {
    "main": {
      "type": "record",
      "description": "An activity that changes inputs into outputs. It could transform or transport economic resource(s).",
      "key": "tid",
      "record": {
        "type": "object",
        "required": ["name"],
        "properties": {
          "name": {
            "type": "string",
            "description": "An informal or formal textual identifier for a process",
            "maxLength": 500
          },
          "hasBeginning": {
            "type": "string",
            "format": "datetime",
            "description": "The planned beginning of the process"
          },
          "hasEnd": {
            "type": "string",
            "format": "datetime",
            "description": "The planned end of the process"
          },
          "finished": {
            "type": "boolean",
            "description": "The process is complete or not. Indicates that no more will be done.",
            "default": false
          },
          "classifiedAs": {
            "type": "array",
            "description": "References concepts in a common taxonomy for categorization",
            "items": {
              "type": "string",
              "format": "uri"
            },
            "maxLength": 100
          },
          "basedOn": {
            "type": "string",
            "format": "at-uri",
            "description": "The definition or specification for a process"
          },
          "deletable": {
            "type": "boolean",
            "description": "The process can be safely deleted, has no dependent information"
          },
          "plannedWithin": {
            "type": "string",
            "format": "at-uri",
            "description": "The process with its inputs and outputs is part of the plan"
          },
          "inScopeOf": {
            "type": "array",
            "description": "Grouping around something to create a boundary or context, used for documenting, accounting, planning",
            "items": {
              "type": "string",
              "format": "at-uri"
            },
            "maxLength": 50
          },
          "note": {
            "type": "string",
            "description": "A textual description or comment",
            "maxGraphemes": 3000,
            "maxLength": 10000
          }
        }
      }
    }
  }
}
