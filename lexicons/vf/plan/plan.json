{
  "lexicon": 1,
  "id": "vf.plan.plan",
  "defs": {
    "main": {
      "type": "record",
      "description": "A logical collection of processes that constitute a body of planned work with defined deliverable(s)",
      "key": "tid",
      "record": {
        "type": "object",
        "required": ["name"],
        "properties": {
          "name": {
            "type": "string",
            "description": "An informal or formal textual identifier for a plan",
            "maxLength": 500
          },
          "created": {
            "type": "string",
            "format": "datetime",
            "description": "The time the plan was made"
          },
          "due": {
            "type": "string",
            "format": "datetime",
            "description": "The time the plan is expected to be complete"
          },
          "refinementOf": {
            "type": "string",
            "format": "at-uri",
            "description": "This plan refines a scenario, making it operational"
          },
          "inScopeOf": {
            "type": "array",
            "description": "Grouping around something to create a boundary or context, used for documenting, accounting, planning",
            "items": {
              "type": "string",
              "format": "at-uri"
            },
            "maxLength": 50
          },
          "note": {
            "type": "string",
            "description": "A textual description or comment",
            "maxGraphemes": 3000,
            "maxLength": 10000
          }
        }
      }
    }
  }
}
