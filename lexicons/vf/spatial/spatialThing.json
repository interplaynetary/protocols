{
  "lexicon": 1,
  "id": "vf.spatial.spatialThing",
  "defs": {
    "main": {
      "type": "record",
      "description": "A physical mappable location",
      "key": "tid",
      "record": {
        "type": "object",
        "required": ["name"],
        "properties": {
          "name": {
            "type": "string",
            "description": "An informal or formal textual identifier for a location",
            "maxLength": 500
          },
          "mappableAddress": {
            "type": "string",
            "description": "An address that will be recognized as mappable by mapping software",
            "maxLength": 1000
          },
          "lat": {
            "type": "integer",
            "description": "Latitude (scaled integer, divide by 10^7 for decimal degrees)"
          },
          "long": {
            "type": "integer",
            "description": "Longitude (scaled integer, divide by 10^7 for decimal degrees)"
          },
          "alt": {
            "type": "integer",
            "description": "Altitude in meters (scaled integer, divide by 10^2 for decimal meters)"
          },
          "note": {
            "type": "string",
            "description": "A textual description or comment",
            "maxGraphemes": 3000,
            "maxLength": 10000
          }
        }
      }
    }
  }
}
