{
  "lexicon": 1,
  "id": "vf.agent.organization",
  "defs": {
    "main": {
      "type": "record",
      "description": "A formal or informal group, or legal organization with economic agency",
      "key": "tid",
      "record": {
        "type": "object",
        "required": ["name"],
        "properties": {
          "name": {
            "type": "string",
            "description": "The name that this agent will be referred to by",
            "maxLength": 500
          },
          "image": {
            "type": "string",
            "format": "uri",
            "description": "The URI to an image relevant to the agent, such as a logo"
          },
          "classifiedAs": {
            "type": "array",
            "description": "References one or more concepts in a common taxonomy or other classification scheme for purposes of categorization or grouping",
            "items": {
              "type": "string",
              "format": "uri"
            },
            "maxLength": 100
          },
          "note": {
            "type": "string",
            "description": "A textual description or comment",
            "maxGraphemes": 3000,
            "maxLength": 10000
          }
        }
      }
    }
  }
}
